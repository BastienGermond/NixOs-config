groups:
  - name: gatus
    rules:
      - alert: GatusServiceDown
        expr: gatus_results_total{success="false"}
        for: 5m
        labels:
          severity: warning
        annotations:
          text: |
            Gatus monitoring indicate that {{ $labels.name }} is down since 5 minutes.
